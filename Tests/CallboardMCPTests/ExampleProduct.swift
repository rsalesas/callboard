import CallboardTransport

/// The product these tests stand in for. Callboard knows no product by name;
/// every name, path and variable it uses comes from a value like this one.
let example = Product(name: "Example", command: "example", environmentPrefix: "EXAMPLE")
